---
driver:
  name: vagrant

provisioner:
  name: chef_zero
  data_bags_path: test/data_bags
  environments_path: test/environments

platforms:
  - name: ubuntu-14.04

suites:
  - name: production
    provisioner:
      client_rb:
        environment: directory-prod
    run_list:
      - recipe[chef_directory::default]
    attributes:
      mysql:
        host: <%= `ipconfig getifaddr en0` %>
        password: directory
      #  username: member_directory
      #  database: member_directory_production
      deployment:
        revision: 6fbe4f4fc057ea0ba21520e7967994156e1f7813
